import React from 'react'
import settings from '../../settings.json';


function Matriculas(props) {
  const urlBase = `${settings.api.baseUrl}/matriculas/create`;

//  const [matriculas, setMatriculas] = useState([]);

  return (
    <div>Matriculas</div>
  )
}

export default Matriculas